package main

import "fmt"


func main() {
    var str = "Hannah"
    runes := []byte(str)

    fmt.Println("----------")
    fmt.Printf("runes: %v\n", runes)
    for i,j := 0, len(runes)-1; i < len(runes)/2; i,j = i+=1, j-=1 {
        pron
    }
}
